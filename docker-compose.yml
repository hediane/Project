version: '3.7'
services:
 projet:
  build:
   context : ./Mvc/
   dockerfile : dockerfile
  container_name: deploy_projet
  expose :
   - "5000"
  depends_on:
   - nginx
  restart : always

 nginx:
  image: nginx:alpine
  volumes:
   - "./nginx.conf:/etc/nginx/nginx.d"
  ports:
   - "4001:80"
  restart : always



